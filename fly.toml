# fly.toml app configuration file generated for oman-lead-bot on 2026-08-12T02:39:36Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'oman-lead-bot'
primary_region = 'ams'

[build]

[env]
  PORT = '8080'

[processes]
  app = 'run -A ./main.ts'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
